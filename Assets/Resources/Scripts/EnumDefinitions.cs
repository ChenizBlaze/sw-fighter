using UnityEngine;
using System.Collections;

public class EnumDefinitions : MonoBehaviour {

	enum Direction { Up, Down, UpLeft, UpRight, DownLeft, DownRight, Left, Right };


	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
